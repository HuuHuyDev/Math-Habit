<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.KidsApp">


        <!-- Parent Main Activity (Launcher) -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".ui.parent.main.ParentMainActivity"
            android:exported="false" />

        <!-- Child Main Activity -->
        <activity
            android:name=".ui.child.main.ChildMainActivity"
            android:exported="true" />

        <!-- Challenge Activities -->
        <activity
            android:name=".ui.challenge.JoinByChallengeCodeActivity"
            android:exported="false"
            android:theme="@style/Theme.KidsApp.Dialog" />

        <activity
            android:name=".ui.challenge.ChallengeActivity"
            android:exported="false" />

        <activity
            android:name=".ui.challenge.QuickMatchActivity"
            android:exported="false" />

        <!-- Invite Friends Activity -->
        <activity
            android:name=".ui.challenge.InviteFriendsActivity"
            android:exported="false" />


    </application>

</manifest>
